```bash
source jittor/bin/activate
```

```bash
cd /mnt/d/study/competion
```

本地配置github

1.本地配置git账户

```bash
git config --global user.name "33good"
```

2.绑定github邮箱

```bash
git config --global user.email "2295449605@qq.com"
```

3.与远程github建立连接

```bash
git remote add origin https://github.com/33good/-.git
```

```bash
ubuntu config --default-user vamos
```

```bash
source ~/jittor/bin/activate
```



### 更新github

```cmd
cd C:\raytron_project
git add .
git commit -m "更新了数据预处理脚本"
git push origin master
```

https://github.com/nyz-balabababa/rayton

获取代码

```
git clone https://github.com/nyz-balabababa/rayton.git
```



### 打榜提交：

拉镜像：

```powershell
docker pull supervisely/sam3:1.0.6
```

```powershell
docker run --rm --gpus all -it `
  -v "D:\nyz\sam3\选手示例工程\ESAM-11cls-stage2-mild-v2:/raytron/code" `
  -v "D:\nyz\sam3\选手示例工程\ESAM-11cls-stage2-mild-v2\datasets:/raytron/test" `
  supervisely/sam3:1.0.6 `
  bash -lc "python3 -m pip install -r /raytron/code/requirements.txt && python3 /raytron/code/inference.py"
```

